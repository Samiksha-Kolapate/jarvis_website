 /* partners swiper strated */
 // Initialize Swiper

 var swiper = new Swiper('.swiper', {
    direction: 'horizontal',
    loop: true,
 
    spaceBetween: 30,
    autoplay:{
      delay:4000,

    },
    // If we need pagination
    pagination: {
        el: '.swiper-pagination',
        clickable: true,
    },
});

 /* partners swiper ended */

 /* navbar menu display started */ 
function showMenu(){
  const navList = document.querySelectorAll('.nav-list');
  
  }
  
  /* navbar menu display ended */ 
   